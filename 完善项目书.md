# 末日生存文字游戏平台｜完善项目书（v1）

## 1. 文档目标
- 明确“当前已落地能力”与“最终目标”之间的差距。
- 给出可执行的完善路线、验收标准与风险控制策略。
- 统一代码实现、接口文档、对外描述三者口径。

## 2. 当前已落地能力（基线）
- 架构与编排：Spring Boot 3 + Spring AI Alibaba + Vue3，Agent Harness 责任链（Router -> Retrieval -> Difficulty -> Plot -> Option -> RuleGuard -> StateCommit -> Narration）。
- 状态一致性：Redis 会话状态 + 版本号乐观锁 + 幂等键防重。
- 数据与检索：PostgreSQL + pgvector，已具备混合召回（关键词 + 向量）与 L0/L1 记忆沉淀。
- 可观测：AOP 采集 Agent 耗时、阶段耗时、Token 指标，支持 trace 明细查询；Admin 页面可查看聚合指标和链路明细。
- 多模态最小闭环：`/media/images/generate`（文生图优先）+ 自动回退 `/media/images/gallery-search`（Pexels）。
- 工程化：Docker Compose 联调、Flyway 迁移、基础故障注入能力。

## 3. 与最终目标的差距清单

### 3.1 可观测与运维体系差距
- 现状：有指标采集与管理端查询，但尚未形成 Prometheus + Grafana 长周期看板与告警闭环。
- 差距：缺少线上 SLA 看护能力（P95、错误率、回退率异常无法自动告警）。
- 影响：问题发现滞后，难以对性能退化做及时止损。

### 3.2 时延治理差距
- 现状：已能采集 queue/model/post_process 分段指标。
- 差距：尚未形成“模板命中优先 + 上下文瘦身 + 非关键异步化 + 缓存”的系统优化闭环。
- 影响：高峰时段 P95 波动仍可能偏大，体验不稳定。

### 3.3 冲突仲裁与投票策略差距
- 现状：已有 RuleGuard + fallback 机制。
- 差距：高风险投票规则（riskScore >= 0.7）尚未体系化上线并完成灰度验证。
- 影响：关键决策质量与时延成本尚未达到最优平衡。

### 3.4 RAG 与记忆量化评估差距
- 现状：已实现分层记忆和混合召回。
- 差距：缺少稳定的离线评测集（已定 100 条）与自动评估报告产线。
- 影响：优化收益难以复现与对比，技术陈述证据不足。

### 3.5 多模态生产化差距
- 现状：文生图 + 图库兜底已打通，失败可回退。
- 差距：缺少内容安全审查、版权策略、缓存与成本治理、前端体验统一策略。
- 影响：可用但不够“可运营”。

### 3.6 API 口径与文档一致性差距
- 现状：核心接口可用，文档覆盖较全。
- 差距：少量“规划中/已实现”状态需要持续同步，避免对外描述不一致。
- 影响：联调和汇报成本增加。

## 4. 完善路线（按优先级）

### P0：运行稳定与可观测闭环（必须先做）
- 接入 Prometheus 抓取核心指标：
  - `doomsday.agent.handle.latency`
  - `doomsday.agent.stage.latency`
  - `doomsday.agent.llm.tokens`
- 落地 Grafana 面板：总时延、阶段占比、Token 趋势、错误率、fallback 率。
- 告警阈值：`P95 > 10s`、`error_rate > 3%`、`fallback_rate` 连续异常升高。
- 验收：出现慢请求可通过 traceId 在 5 分钟内定位瓶颈阶段。

### P1：时延治理与容量稳定
- 意图模板化优先（目标覆盖 60% 常见意图），未命中再走 LLM。
- 检索前置过滤（location/tag）减少无效上下文。
- 非关键写回异步化（低优先摘要、统计日志）。
- 热点检索与规则校验短 TTL 缓存。
- 验收：稳定达成 `P95 <= 10s`，同时质量指标不退化。

### P2：高风险投票策略上线
- 风险识别：大额资源变更、关键剧情分叉、濒危状态。
- 投票触发：仅当 `riskScore >= 0.7` 启用。
- 灰度策略：按场景开启，保留全局开关与快速回滚。
- 验收：冲突率下降，且时延增幅在可接受范围。

### P3：离线评测集与质量量化
- 建立 100 条离线样本（意图、召回、一致性、规则命中、时延）。
- 形成固定评估脚本和对比报告（CSV + Markdown）。
- 指标口径：Recall@K、冲突率、平均 token/turn、P95。
- 验收：每次策略变更可自动产出“前后对比”。

### P4：多模态生产化增强
- 文生图超时阈值默认 3s，失败自动回退图库。
- 增加图片缓存、内容审核、版权信息透传、失败重试上限。
- 前端统一展示协议：`source`、`provider`、`fallback`、`fallbackReason`。
- 验收：主流程配图可用率 >= 99%，失败可追踪、可解释。

## 5. 重要风险自查
- 外部依赖风险：DashScope 权限与稳定性不确定，需保持图库兜底与监控告警。
- 数据一致性风险：并发写冲突需严格依赖 `expectedVersion` 与幂等键。
- 成本风险：Token 增长和图片接口调用量上升，需按场景限流与缓存。
- 安全合规风险：图片版权与内容安全策略需上线前补齐。
- 运维风险：若仅依赖 Redis 临时指标，历史趋势与回溯能力不足。

## 6. 交付清单（建议）
- 一份可观测大盘（Grafana）与告警规则配置。
- 一份离线评测报告模板与自动化脚本。
- 一份多模态生产策略说明（超时、降级、缓存、审核、成本）。
- 一份接口实现状态清单（已实现/规划中）并与 API 文档同步。

## 7. 对外陈述口径（简历/汇报）
- 只陈述已被日志、指标、报告验证的数据。
- 强调“工业化落地能力”：编排、可观测、容错降级、工程化部署。
- 对“规划中能力”明确标注状态，避免夸大。

## 8. 当前阶段结论
- 项目已从“可运行 MVP”进入“可运营工程化”阶段。
- 下一阶段核心不是继续铺功能，而是补齐可观测、时延治理、评测量化与生产合规闭环。
