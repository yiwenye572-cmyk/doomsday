# 末日生存文字游戏平台｜全栈计划书（Spring Boot 3 + Spring AI Alibaba + Vue3）

## 1. 需求对齐

### 1.1 项目目标
- 构建一个可持续运营的末日生存文字游戏平台，支持多智能体协作叙事、状态一致性控制、长期记忆与低成本高质量推理。
- 在保证世界观一致性的前提下，提升剧情生成效率与玩家沉浸感。
- 通过离线预处理 + 在线轻量路由，降低在线推理 token 消耗与响应时延。

### 1.2 MVP 验收标准（建议）
- 单局稳定运行 60 分钟以上，无状态错乱。
- 至少 3 个智能体协同，关键主线流程无冲突中断。
- 世界观冲突率 <= 3%。
- 平均响应时延 P95 <= 2.5s（文本模式）。
- 在线 token 消耗相对纯在线生成模式下降 >= 40%。

### 1.3 非目标（首版）
- 不追求复杂开放世界 3D 玩法。
- 不追求全量多模态能力一次性上线。
- 不做无限制自由输入，首版采用“自由文本 + 指令约束”的混合输入。

---

## 2. 总体方案与模块边界

## 2.1 架构分层
- 前端层（Vue3）：对话 UI、状态面板、背包与属性、日志回放。
- 编排层（Agent Harness）：中心化编排器 + 责任链 + 局部并行调度。
- 记忆与检索层：L0/L1/L2 分层记忆 + PGvector + 标签过滤 + 重排。
- 状态层：Redis 全局状态中心 + 版本号 + 乐观锁 + 幂等控制。
- 知识层：离线世界观工厂产出的结构化知识库（规则、实体、事件卡片、Lorebook）。
- 可观测层：AOP 埋点 + TraceID + 指标面板 + 告警。

## 2.2 在线与离线职责划分
- 离线负责：世界观说明书清洗、结构化提取、事件卡片构建、实体状态机定义、向量索引生成。
- 在线负责：轻量路由、多路召回、规则校验、局部生成补全、冲突仲裁、状态持久化。

---

## 3. 离线世界观工厂预处理模块（重点）

> 目标：在游玩前把“模糊叙事知识”转成“可执行结构化知识”，显著减少在线歧义和重复推理成本。

## 3.1 输入与输出
- 输入：AI 生成的世界观说明书（怪物规则、物品数值、事件卡片草稿、阵营关系、地图设定）。
- 输出：
  - 结构化剧情事件库（Event Schema）
  - Lorebook 标签体系（主题、地区、势力、时间、危险等级）
  - 实体状态机定义（玩家/NPC/怪物/据点）
  - 规则 DSL（触发条件、约束、冲突优先级）
  - 向量索引与关键词倒排索引

## 3.2 处理流水线
1. 文档切分与语义单元识别
- 按章节、设定类型、剧情节点切分，不用固定字数切块。
- 识别实体定义段、规则段、事件段、数值段。

2. 结构化提取（LLM + 规则引擎）
- LLM 抽取 JSON 草稿。
- 规则引擎做字段校验（必填、枚举、数值区间、引用完整性）。
- 失败样本进入人工审核池或自动修复重试。

3. Lorebook 标签化构建
- 自动标签：地区、势力、事件类型、风险等级、前置章节。
- 人工标签：剧情风格、叙事语气、禁用主题。
- 建立标签词典与同义词映射，支持检索扩展。

4. 实体状态机预定义
- 示例：怪物状态（潜伏 -> 警戒 -> 追击 -> 撤退/死亡）。
- 每个状态转移绑定触发条件、冷却、优先级、禁止条件。
- 输出可执行状态图（JSON/YAML）。

5. 规则编译与索引构建
- 将规则 DSL 编译为可快速匹配的条件表达式。
- 构建 PGvector 向量索引 + Redis 热点缓存 + 倒排索引。

## 3.3 建议数据模型（核心表）
- world_doc(id, version, source, content, status, created_at)
- entity_def(entity_id, type, attrs_json, version)
- state_machine(entity_type, state_graph_json, version)
- lorebook_entry(entry_id, title, body, tags_json, priority, version)
- event_card(event_id, trigger_json, effect_json, constraints_json, rarity, version)
- rule_dsl(rule_id, scope, expr, priority, action, version)
- embedding_chunk(chunk_id, ref_type, ref_id, text, vector, tags_json)

## 3.4 提效与消歧机制
- 规则匹配提效：规则预编译 + 标签倒排 + 热点缓存，目标提升 60%+。
- 事件触发消歧：
  - 先硬规则过滤（可触发/不可触发）
  - 再语义相似度排序
  - 最后仲裁器定稿（规则优先，语义次之，投票兜底）

---

## 4. 你提出的“类 RAG 模块”可行性评估

## 4.1 结论
- 方案可行，而且非常适合这个项目。
- 只要把“全量在线生成”改成“离线沉淀 + 在线检索 + 小模型路由 + 必要时大模型补全”，就能显著节省 token。

## 4.2 推荐在线推理链路（省 token 版本）
1. 轻量路由 LLM（小模型）
- 判断请求类型：规则查询、事件推进、战斗结算、叙事润色。
- 只做路由与参数提取，不生成长文本。

2. 多路召回
- 路由 A：预存事件卡片召回（高优先级，低成本）
- 路由 B：Lorebook/规则库召回（确保世界观一致）
- 路由 C：历史记忆召回（L0/L1）
- 路由 D：实时生成候选（可选，仅在召回不足时触发）

3. 候选融合与冲突仲裁
- 硬规则先过滤。
- 语义对齐重排。
- 多智能体投票或主控智能体拍板。

4. 最终输出生成
- 优先模板化/卡片化拼装。
- 只有在需要创意过渡时调用较大模型做“局部改写”，避免全量重写。

## 4.3 为什么会省 token
- 大段背景和规则不再每轮塞入上下文。
- 高频情节直接命中事件卡片，减少“从零生成”。
- 小模型承担路由与分类，大模型仅处理高价值复杂段落。
- 历史内容先摘要（L1）再注入，避免长上下文膨胀。

## 4.4 可能踩坑与规避
- 坑 1：事件卡过于僵硬，剧情像拼接。
  - 规避：加入“变体槽位 + 语气层 + 环境扰动因子”。
- 坑 2：召回很多但彼此冲突。
  - 规避：统一版本与优先级体系，先规则后语义。
- 坑 3：小模型路由误判。
  - 规避：低置信度自动回退到主模型路由。

---

## 5. 在线编排与一致性设计

## 5.1 Agent Harness 编排
- 中心化 Orchestrator 管理回合生命周期。
- 责任链节点示例：输入规范化 -> 路由 -> 检索 -> 仲裁 -> 状态写入 -> 叙事输出。
- 主线串行，支线可并行（情报收集、素材检索、配图生成）。

## 5.1.1 Agent 角色设计（你关心的重点）
- Router Agent（路由智能体）
  - 职责：识别玩家输入意图，决定走规则查询、事件推进、战斗结算还是自由探索。
  - 输入：player_input + session_state + recent_memory。
  - 输出：intent, confidence, next_agents。

- Retrieval Agent（检索智能体）
  - 职责：执行多路召回（事件卡、Lorebook、L0/L1 记忆、规则库），返回候选证据集。
  - 输入：intent + query_slots + tags。
  - 输出：candidate_contexts（含来源、分数、版本）。

- Plot Generation Agent（剧情生成智能体）
  - 职责：在规则约束下生成“本回合叙事结果”，只负责剧情推进，不负责给玩家选项。
  - 输入：candidate_contexts + world_rules + state_snapshot。
  - 输出：plot_result（剧情文本、触发事件、状态变更建议、证据引用）。

- Option Generation Agent（选项生成智能体）
  - 职责：根据剧情结果固定生成 4 个可执行选项（激进/稳健/资源导向/探索导向），并给出预估后果。
  - 输入：plot_result + player_profile + state_snapshot。
  - 输出：options[]（option_text, risk_level, expected_effect, required_items）。

- Rule Guard Agent（规则守卫智能体）
  - 职责：校验剧情与选项是否违反世界观硬规则、数值边界、状态机转移约束。
  - 输入：plot_result + options[] + rule_dsl + state_machine。
  - 输出：validation_result（pass/fail + violations + fix_suggestions）。

- State Commit Agent（状态提交智能体）
  - 职责：将仲裁后的变更写入 Redis 与持久层，做版本校验、幂等控制和补偿。
  - 输入：approved_delta + current_version + request_id。
  - 输出：commit_result（new_version, committed_fields, rollback_token）。

- Narration Agent（文风润色智能体，可选）
  - 职责：对最终文案做风格统一与可读性优化，不改变事实状态；强制二人称“你”叙事。
  - 输入：approved_plot + style_profile。
  - 输出：final_narration。

## 5.1.2 单回合协作顺序（建议默认链路）
1. Router Agent 判定意图与链路。
2. Retrieval Agent 执行多路召回。
3. Plot Generation Agent 生成剧情草案。
4. Option Generation Agent 生成玩家可选动作。
5. Rule Guard Agent 做规则与状态机校验。
6. Orchestrator 执行冲突仲裁（必要时触发二次生成）。
7. State Commit Agent 提交状态。
8. Narration Agent 输出最终文案。

## 5.1.3 关键设计原则
- 职责单一：剧情生成与选项生成拆分，避免一个 Agent 同时做叙事和决策引导。
- 可替换：每个 Agent 通过统一 I/O 契约接入，支持无侵入替换模型。
- 可回放：保留每个 Agent 的输入摘要、输出与证据引用，便于复盘与调试。
- 低成本：Router/Option 优先用轻量模型，Plot 仅在必要时升级大模型。

## 5.1.4 文本风格与长度规范
- 叙事人称：统一使用第二人称“你”。
- 文本长度：每轮剧情文本建议 220-420 字，采用“环境描写 + 行动反馈 + 风险暗示 + 钩子收束”结构。
- 文风目标：接近末日求生小说，强调压迫感、资源稀缺感与感官细节。
- 事实约束：文风润色不得新增未被规则或状态支持的事实。

## 5.1.5 剧情与选项 Agent 契约示例
1. Plot Generation Agent 输入示例
```json
{
  "sessionId": "s_1001",
  "turn": 27,
  "playerAction": "我尝试绕到加油站后门搜刮药品",
  "stateSnapshot": {
    "hp": 62,
    "stamina": 35,
    "location": "old_gas_station",
    "inventory": ["knife", "bandage"]
  },
  "retrievedContexts": [
    {"source": "event_card", "id": "ev_033", "score": 0.91},
    {"source": "lorebook", "id": "lb_zone_7", "score": 0.86}
  ],
  "rules": ["night_noise_risk", "zombie_smell_alert"],
  "outputStyle": "grim_realism"
}
```

2. Plot Generation Agent 输出示例
```json
{
  "plotText": "你贴着油罐阴影前行，门闩半断，室内有拖拽痕迹。",
  "triggeredEvents": ["ev_033_variant_b"],
  "proposedStateDelta": {
    "noise": 12,
    "stamina": -6,
    "flags": ["found_medical_trace"]
  },
  "citations": ["event_card:ev_033", "lorebook:lb_zone_7"],
  "confidence": 0.84
}
```

3. Option Generation Agent 输出示例
```json
{
  "options": [
    {
      "id": "opt_a",
      "text": "撬开储物柜，快速搜药后撤离（激进）",
      "riskLevel": "high",
      "expectedEffect": "可能获得稀有药品，但噪音显著上升",
      "requiredItems": ["knife"]
    },
    {
      "id": "opt_b",
      "text": "原地观察 30 秒，先确认是否有感染者巡逻（稳健）",
      "riskLevel": "medium_low",
      "expectedEffect": "降低遭遇战概率，但推进速度略慢",
      "requiredItems": []
    },
    {
      "id": "opt_c",
      "text": "拆解附近废铁与布料，先做简易防护（资源导向）",
      "riskLevel": "medium",
      "expectedEffect": "短期收益一般，但可提升后续生存容错",
      "requiredItems": []
    },
    {
      "id": "opt_d",
      "text": "沿油站后墙潜行，尝试发现隐藏入口（探索导向）",
      "riskLevel": "medium_high",
      "expectedEffect": "可能触发支线与高价值线索，也可能遭遇伏击",
      "requiredItems": []
    }
  ],
  "strategyHint": "当前体力偏低，建议优先稳健路线"
}
```

## 5.2 Redis 状态一致性
- Key 设计：game:{sessionId}:state + version。
- 更新策略：CAS（compare-and-set）+ 乐观锁重试。
- 幂等策略：requestId 去重，防重复提交。
- 补偿机制：状态写入失败时触发补偿命令回滚副作用。

## 5.3 冲突仲裁器（三层）
- 第 1 层：规则校验（硬约束，不可违背）
- 第 2 层：语义对齐（向量相似度 + 标签一致性）
- 第 3 层：智能体投票（仅处理软冲突）

## 5.4 动态老虎机（Dynamic Challenge Director）

> 目标：让玩家长期处于“有压力但可破局”的挑战区间，避免过难劝退或过易无聊。

## 5.4.1 设计定位
- 动态老虎机不是纯随机惩罚器，而是“挑战与回报调度器”。
- 在不破坏世界观硬规则的前提下，动态调节遭遇强度、资源稀缺度、事件频率、奖励质量。

## 5.4.2 核心输入信号
- 生存压力：hp、stamina、感染值、负面状态数量。
- 资源水平：弹药、药品、食物、关键道具覆盖率。
- 玩家表现：近 N 回合成功率、逃跑率、战斗耗时、错误决策率。
- 叙事节奏：连续平稳回合数、高潮事件间隔、重复事件率。

## 5.4.3 目标区间与调度逻辑
- 玩家开局可自主选择三档难度：求生（稳健）、幸存（标准）、地狱（硬核）。
- 按难度档位设置 challenge_band：
  - 求生： [0.35, 0.55]
  - 幸存： [0.45, 0.70]
  - 地狱： [0.60, 0.82]
- 若当前挑战指数低于下界：提高威胁密度或降低资源掉落，触发中风险事件。
- 若当前挑战指数高于上界：降低连续打击强度，增加恢复窗口与保底资源。
- 采用小步调整，限制单回合变化幅度，避免“过山车”体验。

## 5.4.4 与多 Agent 的协作方式
- 新增 Difficulty Director Agent（难度导演智能体）：
  - 输入：state_snapshot + player_skill_profile + recent_outcomes。
  - 输出：difficulty_delta（threat, loot, event_rate, boss_probability）。
- 接入位置：
  - 在 5.1.2 的第 2 步后、第 3 步前执行。
  - 先由 Retrieval Agent 拿到候选，再由 Difficulty Director Agent 重排候选权重。
- 与现有 Agent 分工：
  - Plot Generation Agent 负责叙事表达。
  - Option Generation Agent 负责可玩决策。
  - Difficulty Director Agent 只负责“难度参数调度”，不直接写剧情文案。

## 5.4.5 反刷与公平性约束
- 禁止读取玩家未来输入，仅基于历史与当前状态调参。
- 同一类高惩罚事件设置冷却，避免连续触发。
- 保护机制：
  - 连续失败 >= 3 回合时，触发保底补给或低风险脱困分支。
  - 连续顺风 >= 5 回合时，提高环境不确定性而非直接数值碾压。
  - 引入“绝境翻盘卡”：一次性免死或强补给，仅在满足濒危条件时触发，且单局限次。

## 5.4.6 最小实现建议（MVP）
1. 先做规则版调度器（无模型）：阈值 + 权重 + 冷却。
2. 稳定后再升级为轻量模型调度（输出 difficulty_delta）。
3. 最后再引入个性化参数（按玩家风格分群）。

---

## 6. 记忆与 RAG 细化

## 6.1 三级记忆
- L0：滑动窗口（最近 N 轮对话与状态变化）
- L1：剧情节点摘要（日记化沉淀，阶段结束写入）
- L2：原始文档（世界观、规则、历史事件全文）

## 6.2 检索策略
- 召回：向量检索 + 标签过滤 + 关键词倒排。
- 排序：规则命中分 + 语义相似度 + 时间衰减。
- 输出：只注入最小必要上下文（TopK + 截断 + 去重）。

## 6.3 记忆写入门控
- 仅关键事件写入 L1（角色死亡、阵营变化、剧情分叉）。
- 重复信息不写入，避免记忆污染。

---

## 7. 工具调用与多模态网关

## 7.1 Tool Calling（ReAct）
- 原子工具：世界观查询、实体状态修改、事件触发校验、物品数值计算。
- 统一返回协议：code, message, data, retryable。
- 失败重试：指数退避 + 最大重试次数 + 补偿事务。

## 7.2 多模态网关
- 策略模式封装：文生图、图库检索、占位图降级。
- 熔断与限流：外部接口异常时自动降级为文字叙述。

---

## 8. 可观测性与运维

## 8.1 指标体系
- 业务指标：剧情冲突率、事件命中率、玩家中断率。
- 系统指标：P95 延迟、错误率、重试率、缓存命中率。
- 成本指标：单回合 token 消耗、单局模型成本。
- 难度平衡指标：
  - 挑战区间命中率（挑战指数落在目标区间的回合占比）。
  - 连续挫败回合数 P95。
  - 高压后恢复成功率（高压事件后 3 回合内存活并推进剧情的比例）。

## 8.2 日志与追踪
- 每回合生成 traceId。
- 记录：路由决策、召回候选、仲裁结果、状态变更 diff。
- 脱敏：日志中屏蔽敏感输入。

## 8.3 异常处理
- 模型超时：降级到规则模板回复。
- 检索失败：回退到最小世界观上下文 + 保守剧情推进。
- 状态冲突：重试失败后进入人工审计队列。

---

## 9. 分阶段实施计划

## 9.1 P0（1-2 周）
- 单智能体回合流程跑通。
- Redis 状态中心 + 版本控制。
- 离线工厂最小版（事件卡结构化 + 基础标签）。

## 9.2 P1（2-3 周）
- Agent Harness 中心编排 + 责任链。
- 多路召回（事件卡 + Lorebook + L0 记忆）。
- 冲突仲裁 v1（规则优先）。

## 9.3 P2（2-3 周）
- L1 剧情日记沉淀。
- PGvector 检索 + 重排。
- 路由小模型上线与置信度回退。

## 9.4 P3（2 周）
- Tool Calling 标准化 + 重试补偿。
- 多模态网关 + 降级策略。
- AOP 可观测与告警。

## 9.5 P4（1-2 周）
- 压测与故障注入。
- 灰度发布与回滚演练。
- 质量验收与项目复盘。

---

## 10. 里程碑指标（建议写入简历/答辩）
- 规则匹配效率提升 >= 60%。
- 在线 token 消耗下降 >= 40%。
- 检索准确率提升 >= 40%。
- 多智能体冲突率下降 >= 50%。
- 事件触发歧义率下降到 <= 2%。
- 挑战区间命中率 >= 65%。
- 连续失败 3 回合以上占比 <= 10%。

---

## 11. 已确认参数（v1）
- 目标体验优先级：偏硬核生存。
- 难度选择机制：玩家开局可自主选择三档难度（求生/幸存/地狱）。
- 保护阈值：连续失败 >= 3 回合触发保底。
- 顺风抑制阈值：连续顺风 >= 5 回合增强不确定性。
- 选项数量：固定 4 个。
- 绝境翻盘卡：进入首版。
- 叙事风格：统一第二人称“你”。
- 每轮剧情长度：偏长文本（建议 220-420 字），贴近末日求生小说风格。

---

## 12. 结论
- 你的方案方向正确，且具备工业化潜力。
- 核心要点是：把高成本推理前置到离线，把在线过程做成“路由 + 检索 + 仲裁 + 局部生成”。
- 这样既能保证世界观一致性，也能显著节约 token，并提升系统稳定性与可维护性。

---

## 13. 自动推进执行进展（2026-05-01）

### 13.1 P2 已落地项
- L1 剧情日记沉淀：已接入 Redis（`game:memory:l1:{sessionId}`），并在新回合注入到上下文。
- PGvector 检索 + 重排：Lorebook 已从纯关键词升级为“关键词 + 向量”混合召回，采用融合评分排序。
- 路由小模型上线与置信度回退：Router 低置信度回退已存在，当前保持稳定策略。

### 13.2 P3 自动推进落地项
- AOP 可观测：新增 Agent 节点耗时指标 `doomsday.agent.handle.latency`（按 agent/status 打标签）。
- 追踪链路：请求级 `X-Trace-Id` 透传，Controller 与全局异常响应统一返回同一 traceId。
- 重试能力：向量检索链路已接入轻量重试执行器（2 次尝试）。

### 13.3 P4 自动推进落地项
- 故障注入：新增可控 failpoint 机制（默认关闭），可通过请求头触发指定阶段失败。
- 演练开关：`game.chaos.enabled=false`，仅在演练环境手动打开。
- 演练目标：验证 submitTurn/chooseOption/comeback 主链路在异常场景下的幂等与回退表现。

### 13.4 验收建议
1. 启用 `game.chaos.enabled=true` 后，使用 `X-Doomsday-Failpoint` 分别演练：
  - `submitTurn.beforeOrchestrator`
  - `submitTurn.afterOrchestrator`
  - `chooseOption.beforeApply`
  - `comeback.beforeApply`
2. 访问 `actuator/metrics` 检查 Agent 指标是否按节点上报。
3. 核对每次请求响应体 traceId 与响应头 `X-Trace-Id` 是否一致。
